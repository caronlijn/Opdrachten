//for (let i = 0; i <= 20; i++) 
for (let i = 0; i <= 20; i = i + 2) {
    console.log(`${i} even, and ${1 +i} is odd`);
    //console.log(`Is ${1 + i} +1 is odd`);
    // TODO: determine if i is odd or even
    // TODO: use console.log
  }
